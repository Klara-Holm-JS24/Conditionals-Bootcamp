//bigger
let a = 1200;
let b = 120;

if (a > b) {
    console.log('a ('+a+') is bigger than b ('+b+').')
} else {
    console.log('a ('+a+') is not bigger than b ('+b+').')
}


//equals or not
a = 'apple';
b = 'pear';

if (a === b) {
    console.log('a ('+a+') and b ('+b+') are the same.')
} else if (a == b) {
    console.log('a ('+a+') and b ('+b+') are sort of the same, but not really.')
} else if (a != b){
    console.log('a ('+a+') and b ('+b+') are not the same.')
}